# 架构概览

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **后端** | Go 1.26 + Gin | RESTful API 服务器 |
| **前端** | Next.js 15 + React 19 | SSR/SSG 前端应用 |
| **数据库** | MySQL 8.0 | 主数据存储 |
| **容器化** | Docker + Docker Compose | 开发与部署环境 |

### 后端核心依赖

- **Gin** — HTTP Web 框架
- **go-sql-driver/mysql** — MySQL 驱动
- **goldmark** — Markdown 渲染
- **gorilla/sessions** — 会话管理
- **google/uuid** — UUID 生成
- **golang.org/x/crypto** — 密码哈希（bcrypt）
- **gopkg.in/yaml.v3** — YAML 配置解析

### 前端核心依赖

- **next-themes** — 主题切换（暗色/亮色）
- **TypeScript** — 类型安全

## 项目目录结构

```
GoKYCH/
├── cmd/gokych/          # 后端入口
│   └── main.go          # 启动流程：配置→数据库→路由→服务
├── internal/            # 后端内部包
│   ├── api/             # HTTP 处理器与路由
│   │   ├── admin/       # 管理后台相关处理器
│   │   ├── router.go    # 路由注册
│   │   ├── server.go    # Server 结构体定义
│   │   ├── middleware.go # 会话/CSRF/权限中间件
│   │   ├── articles.go  # 文章 CRUD
│   │   ├── auth.go      # 登录/登出
│   │   ├── comments.go  # 评论
│   │   ├── ratings.go   # 评分
│   │   ├── search.go    # 全文搜索
│   │   └── site.go      # 站点信息
│   ├── auth/            # 认证模块
│   │   ├── password/    # 密码哈希与校验
│   │   ├── ratelimit/   # 登录速率限制
│   │   ├── session/     # 会话加密与管理
│   │   └── user/        # 用户查询
│   ├── config/          # 配置加载（环境变量 > YAML > 默认值）
│   ├── content/         # 内容业务逻辑
│   │   ├── parsers/     # 标记语言解析器
│   │   │   ├── markdown.go
│   │   │   ├── wikidot.go
│   │   │   ├── bbcode.go
│   │   │   └── render.go
│   │   ├── articles.go
│   │   ├── comments.go
│   │   ├── ratings.go
│   │   └── tags.go
│   ├── core/            # 核心基础设施
│   │   ├── db/          # 数据库连接池
│   │   ├── schema/      # 建表 DDL 与种子数据
│   │   └── settings/    # 站点设置管理
│   └── typst/           # Typst 排版引擎集成
├── web/                 # 前端 Next.js 应用
│   ├── app/             # App Router 页面
│   │   ├── admin/       # 管理后台页面
│   │   ├── auth/        # 登录页
│   │   ├── {md,wikidot,html,bbcode,typst}/ # 各类型文章页
│   │   ├── labels/      # 标签页
│   │   └── search/      # 搜索页
│   ├── components/      # 共享 React 组件
│   ├── lib/             # API 客户端与类型定义
│   └── styles/          # 全局样式
├── data/                # 运行时数据目录
│   ├── settings/        # 配置文件（db.yaml, settings.yml）
│   ├── avatars/         # 用户头像
│   ├── plugins/         # 插件
│   ├── themes/          # 主题
│   └── typst/           # Typst 临时文件
├── configs/             # 配置模板
├── docker-compose.yml   # Docker 编排
└── Dockerfile           # 多阶段构建
```

## 启动流程

```
main()
  ├── 1. config.Load()           — 加载配置（默认值 → .env → YAML → 环境变量）
  ├── 2. cfg.EnsureDataDirs()    — 创建数据目录
  ├── 3. settings.Ensure()       — 确保默认 settings.yml 存在
  ├── 4. coredb.Init(cfg)        — 初始化 MySQL 连接池
  ├── 5. schema.Init(db)         — 执行 DDL 建表
  ├── 6. schema.SeedAdmin()      — 种子默认管理员
  ├── 7. session.New()           — 创建会话管理器
  ├── 8. ratelimit.New()         — 创建速率限制器
  ├── 9. api.NewServer()         — 创建 API Server
  ├── 10. srv.Setup(r)           — 注册路由
  └── 11. httpSrv.ListenAndServe — 启动 HTTP 服务（支持优雅关闭）
```

## 请求处理流程

```
客户端请求
  → 安全响应头中间件 (securityHeaders)
  → 请求 ID 中间件 (requestID)
  → 会话中间件 (sessionMiddleware)
  → 用户加载中间件 (loadUserMiddleware)
  → 路由匹配
  → [可选] CSRF 中间件 (csrfMiddleware)
  → [可选] 权限中间件 (requireAdmin / requireOwner)
  → 业务处理器
```

## 文章内容类型

系统支持 5 种文章类型，每种使用不同的解析器渲染：

| 类型 | 路径前缀 | 解析器 | 说明 |
|------|----------|--------|------|
| Markdown | `/md/` | goldmark | 标准 Markdown |
| Wikidot | `/wikidot/` | 自研解析器 | Wikidot 语法兼容 |
| HTML | `/html/` | 直接渲染 | 原始 HTML |
| BBCode | `/bbcode/` | 自研解析器 | 论坛风格标签 |
| Typst | `/typst/` | typst 引擎 | 排版系统，支持编译为 HTML/PDF |
