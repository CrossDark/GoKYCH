- 所有数据库操作函数均以 `*sql.DB` 为显式依赖参数，避免全局状态。
- 列表查询接口统一返回包含分页元数据（Total, Page, PerPage, TotalPages）的结果结构体。
- 在批量获取文章列表时，使用 `GetTagsForArticlesBatch` 进行 N+1 查询优化，一次性加载所有关联标签。
- 涉及用户身份的操作（如评论、评分）同时接收 `userID` 和 `authorName`，并在数据库层通过唯一键或逻辑判断防止匿名冒充。