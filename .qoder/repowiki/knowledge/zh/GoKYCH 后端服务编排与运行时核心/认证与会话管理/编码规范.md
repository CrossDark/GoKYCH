- 数据库查询统一使用 `database/sql` 原生接口，通过 `Scan` 方法手动映射字段，未引入 ORM 框架。
- 敏感操作（如 CSRF 校验、密码比对）强制使用常量时间比较算法（`subtle.ConstantTimeCompare` 或 `bcrypt.CompareHashAndPassword`）以防止时序攻击。
- 所有涉及用户输入的字符串（用户名、密码）在进入业务逻辑前均执行严格的格式与强度正则校验。
- 错误处理遵循 Go 惯用法，对于“记录不存在”场景统一通过 `errors.Is(err, sql.ErrNoRows)` 进行语义化判断。