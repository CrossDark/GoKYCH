- 采用“先默认、后文件、再环境变量”的配置加载优先级策略，确保部署灵活性。
- 数据库初始化遵循“无库连接 -> 自动建库 -> 带库连接 -> 连接池配置”的标准流程。
- Schema 迁移通过查询 `information_schema` 实现列级别的增量更新，保证多次执行的安全性。
- 全局日志统一使用 `log/slog` 的文本处理器，并根据 `ginMode` 动态切换 Debug/Info 级别。