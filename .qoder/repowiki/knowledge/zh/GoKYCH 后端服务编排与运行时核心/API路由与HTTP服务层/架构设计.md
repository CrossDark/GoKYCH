- 入口点：`server.go` 负责启动HTTP服务器，`router.go` 集中定义路由映射。
- 结构分层：按业务领域拆分为独立处理器文件（如 `auth.go`, `articles.go`, `admin/`），实现关注点分离。
- 横切关注点：通过 `middleware.go`、`security_headers.go` 和 `requestid.go` 实现请求预处理与安全增强。
- 错误处理：`dberr.go` 提供统一的数据库错误到HTTP状态码的转换逻辑。