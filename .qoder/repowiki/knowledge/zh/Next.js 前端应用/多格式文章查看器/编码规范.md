- 所有页面组件均显式声明 `export const dynamic = "force-dynamic"` 以禁用静态生成。
- 动态路由参数 `params` 和 `searchParams` 均定义为 `Promise` 类型并通过 `await` 解析，适配 Next.js 15+ 规范。
- 详情页统一实现 `generateMetadata` 函数，在数据获取失败时提供默认的兜底标题。