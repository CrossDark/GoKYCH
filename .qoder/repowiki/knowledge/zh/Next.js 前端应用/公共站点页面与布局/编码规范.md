- 服务端页面统一设置 `export const dynamic = "force-dynamic"` 以避免静态缓存导致的数据陈旧。
- 需要读取路由参数或查询字符串的客户端页面，将核心逻辑抽取为独立组件并用 `Suspense` 包裹导出默认页。
- 所有页面级元信息通过 `Metadata` 对象集中声明（标题/描述），并在动态路由中使用 `generateMetadata` 生成。
- 数据获取失败时提供安全的回退默认值（空数组/空对象），确保页面在无后端响应时仍可渲染。