- 所有受保护的管理员 API 调用均强制要求传入 `csrf` 参数并设置 `X-CSRF-Token` 请求头
- API 响应类型统一通过 `lib/types.ts` 中的接口定义进行约束，并在 `api.ts` 中通过 `import("./types")` 引用
- 页面组件遵循 Next.js App Router 规范，使用 `page.tsx` 定义路由入口，`layout.tsx` 定义局部布局