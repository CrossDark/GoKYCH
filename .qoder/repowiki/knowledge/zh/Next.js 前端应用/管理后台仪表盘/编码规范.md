- 所有页面组件均声明 "use client" 以支持客户端状态管理与副作用处理。
- 统一使用 `getCsrf()` 获取令牌并作为参数传递给写操作 API，以确保请求安全性。
- 采用 `try-catch` 块包裹异步 API 调用，并通过局部状态变量（如 `msg`）向用户反馈操作结果。
- 列表页普遍采用“折叠表单 + 数据表格”的布局模式，利用 HTML `<details>` 元素实现新建/编辑功能的显隐切换。