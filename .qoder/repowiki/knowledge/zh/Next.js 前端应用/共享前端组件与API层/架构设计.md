- 采用三层结构：`web/components/`负责现态UI渲染（如Header, ArticleCard），`web/lib/`封装数据访问（api.ts）与领域模型（types.ts），`web/styles/`管理全局样式。
- 组件通过`lib/api.ts`统一发起HTTP请求，并在服务端渲染时自动透传Cookie以维持会话。
- `lib/types.ts`定义了前后端共享的数据契约，确保类型安全。
- 样式采用CSS变量系统，支持通过`ThemeProvider`和`next-themes`实现动态主题切换。