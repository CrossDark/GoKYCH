# 跨越晨昏 — PyKYCH → GoKYCH 重写方案

> 源项目：`../PyKYCH`（Python + LiHiL + Jinja2，约 11.9k 行，SSR）
> 目标项目：`GoKYCH`（**Go + Gin JSON API** + **Next.js (React) 前端**）
> 本文是**实施前的方案设计**，不是已完成记录。

---

## 0. TL;DR

把 PyKYCH 重写为**前后端分离**架构：

- **后端** Go + Gin，只提供 **RESTful JSON API** + Typst PDF 二进制下载 + 静态文件（头像/上传图）。Markdown/Wikidot/BBCode 在**后端渲染成 HTML 字符串**放进 JSON 返回。
- **前端** Next.js (React)，重写 PyKYCH 的 32 个 SSR 页面为 React 页面/组件，保持用户打开网站的视觉与交互流程基本不变。
- **核心业务（5 类文章 / 评论 / 行评论 / 评分 / 标签 / 搜索 / 管理后台 / 认证）100% 功能对齐**。
- 数据库**全面重构**（5 文章表→1、关联表外键化），密码改 **bcrypt**，丢弃现有全部网站设置与用户数据。
- **插件系统 / 主题系统 / 外部 HTML 抓取首版排除**。
- **API 契约全新设计**，不迁就 PyKYCH 现有的 fetch 调用模式。

| 维度 | 选择 |
|------|------|
| 后端 | Go 1.26 + Gin + database/sql + go-sql-driver/mysql |
| 前端 | Next.js (React) + TypeScript |
| 认证 | JWT（access + refresh） |
| 内容渲染 | 后端渲染 HTML（MD/Wikidot/BBCode→HTML 字符串，前端 `dangerouslySetInnerHTML`） |
| 数据库 | MySQL 8，全新 schema，全面重构 |
| 密码 | bcrypt |

---

## 1. 现状摸底（来自 PyKYCH）

### 1.1 规模

| 维度 | 数量 |
|------|------|
| Python 源文件 | 43 个（不含 `__pycache__`） |
| 代码行数 | 约 11,892 行 |
| HTTP 端点 | 约 217 个（admin 占 65） |
| 数据库表 | 15 张 + 1 视图 `VW_ALL_ARTICLES` |
| SSR 页面/模板 | 32 个，约 6,706 行 Jinja2 |
| 静态资源 | 仅 `img/`；**CSS 全部内联在 `base.html`（1011 行）**，无独立 JS/CSS 文件 |

### 1.2 Python 版技术债（重构消除对象）

| 技术债 | 成因 | 重构后 |
|--------|------|--------|
| 5 张同构文章表 | 历史逐类型建表 | 合并 1 张 `articles` + `type` 字段 |
| `VW_ALL_ARTICLES` UNION 视图 + 搜索 5 路 UNION | 跨类型查询 | 单表查询，视图删除 |
| 关联表 `(article_type, article_slug)` 复合键 | 无统一文章 id | 改 `article_id` 外键 |
| `list_recent_articles` 拼 5 段 UNION | 同上 | 一句 `ORDER BY` |
| PBKDF2 自定义格式 + NFC + 手写解析 | 兼容旧哈希 | bcrypt |
| `_migrate_*` 迁移函数 | 在线平滑升级 | 全新库，建表即终态 |
| 评分 API 文档写 0-5、实际是 [-1,1] | 注释笔误 | 统一为 [-1,1]，文档对齐 |
| 行评论/全文评论两张表 | 历史分离 | 合并 `comments`（`line_number` 区分） |
| 外部 HTML 抓取（README 声明） | 代码已删但文档残留 | 彻底移除，HTML 类型保留为纯手写 |

### 1.3 关键技术依赖对照

| 能力 | Python 实现 | Go / Next.js 对应 |
|------|------------|-------------------|
| Web 框架 | LiHiL (ASGI) | Gin |
| 页面渲染 | Jinja2 SSR | **Next.js (React)** |
| 数据库 | aiomysql 异步池 | **database/sql + go-sql-driver/mysql** |
| Markdown | python-markdown + 5 扩展 | **Goldmark + 扩展**（后端渲染） |
| Wikidot 解析 | 自写 18 正则 | Go 重写（后端渲染） |
| BBCode 解析 | 自写 | Go 重写（后端渲染） |
| Typst 渲染 | subprocess 调 typst CLI | os/exec 调 typst CLI（后端） |
| 密码哈希 | PBKDF2-SHA256 | **bcrypt** |
| 会话/认证 | Starlette SessionMiddleware（cookie） | **JWT（access + refresh）** |
| CSRF | session token | **不再需要**（JWT + SameSite，见 §4） |
| WebAuthn | cryptography + 自写 CBOR | **go-webauthn/webauthn** |
| 速率限制 | 进程内 dict | golang.org/x/time/rate |
| 配置 | PyYAML | yaml.v3 + env |
| HTTP 客户端（已弃用） | aiohttp 抓取 | 删除 |

---

## 2. 总体架构

```
┌─────────────────────────────┐        ┌──────────────────────────────┐
│   浏览器                     │        │   GoKYCH 后端 (Gin)            │
│                             │        │                              │
│  Next.js App                │  HTTPS │  /api/v1/*  JSON             │
│   - SSR/SSG 页面             │ ──────▶│   认证 / 文章 / 评论 / 评分 …   │
│   - 客户端交互(评分/行评论)   │  JSON  │   MD/Wikidot/BBCode → HTML   │
│   - 管理后台 SPA             │        │  /static/*  头像/上传图       │
│   (静态导出 / Node 托管)      │        │  /api/v1/.../pdf  二进制      │
└─────────────────────────────┘        │                              │
        fetch 带 JWT                     │         ┌────────────┐       │
                                       │────────▶│  MySQL 8   │       │
                                       │         └────────────┘       │
                                       │   typst CLI (子进程)          │
                                       └──────────────────────────────┘
```

**职责划分**：
- **后端**：业务逻辑、数据库、内容渲染（MD/Wikidot/BBCode→HTML）、Typst 编译、文件存储、认证签发 JWT。**不含任何 HTML 页面渲染**。
- **前端**：所有页面 UI、路由、客户端状态、把后端返回的 HTML 字符串用 `dangerouslySetInnerHTML` 贴进 DOM。文章内容的 CSS（代码高亮、 admonition 等）由前端提供。

**部署形态（推荐）**：Next.js 静态导出（`next export`）由 Go 服务用 `embed` 或 `gin.Static` 托管，**单二进制 + 单端口**，运维最简；也可独立部署前端（Node/Nginx），后端跨域开放。两种形态代码相同，仅构建/部署脚本不同。

---

## 3. 后端设计（Go + Gin）

### 3.1 代码组织（Go 标准布局）

```
GoKYCH/
├── cmd/gokych/main.go              # 入口：配置、DB、路由、Gin 启动
├── internal/
│   ├── config/                     # env + yaml 配置
│   ├── core/
│   │   ├── db/                     # database/sql 连接池 + 自动建库
│   │   ├── schema/                 # 全新建表（见 §4）
│   │   ├── settings/               # settings 读写
│   │   └── sitesettings/           # 子站点链接、推荐文章
│   ├── auth/
│   │   ├── jwt/                    # access + refresh 签发/校验
│   │   ├── password/               # bcrypt
│   │   ├── ratelimit/              # 登录限流
│   │   ├── user/                   # 用户 CRUD + 角色
│   │   ├── profile/                # 资料/头像
│   │   └── webauthn/               # go-webauthn
│   ├── content/
│   │   ├── articles/               # 统一 CRUD（单表，type 区分）
│   │   ├── tags/ comments/ ratings/ files/ notifications/
│   │   └── parsers/
│   │       ├── markdown/           # Goldmark → HTML
│   │       ├── wikidot/            # 18 正则重写 → HTML
│   │       └── bbcode/             # 重写 → HTML
│   ├── typst/                      # os/exec 调 typst + 缓存
│   ├── api/                        # Gin handlers（v1）
│   │   ├── auth.go articles.go comments.go ratings.go
│   │   ├── tags.go search.go files.go admin.go users.go
│   │   ├── settings.go notifications.go sitesettings.go
│   │   ├── typst.go static.go
│   │   └── middleware.go           # JWT 解析、限流、CORS
│   └── render/                     # 渲染调度（按 type 选 parser）
├── web/                            # 仅供 embed 的静态资源（头像等）
├── data/                           # 运行时数据（gitignore）
├── configs/ db.yaml.example .env.example
├── go.mod go.sum
├── Dockerfile docker-compose.yml
└── README.md
```

### 3.2 API 设计原则

- 全部挂 `/api/v1` 前缀，纯 JSON（`Content-Type: application/json`）。
- 统一响应信封：
  ```json
  { "data": <payload|null>, "error": {"code":"...","message":"..."}|null }
  ```
- 分页统一 `?page=1&per_page=10`，返回 `{ items, total, page, per_page, total_pages }`。
- 文章详情返回渲染好的 HTML：
  ```json
  { "data": {
      "id": 1, "type": "md", "slug": "hello", "title": "...",
      "html": "<p>已渲染的 HTML</p>",
      "raw_content": "# 原始 markdown（仅作者/管理员可见，供编辑）",
      "author": {...}, "created_at": "...", "updated_at": "...",
      "tags": [...], "can_edit": true
  }}
  ```
- 需登录的接口由 JWT 中间件保护，401 返回 `{error:{code:"unauthorized"}}`。
- 错误码 + HTTP 状态对齐（400 参数/401 未认证/403 无权限/404 不存在/409 冲突/429 限流/500）。

### 3.3 API 路由总表（核心，按资源分组）

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| **认证** | | | |
| POST | `/api/v1/auth/login` | 账密登录 → access+refresh | 公开 |
| POST | `/api/v1/auth/refresh` | refresh 换新 access | refresh |
| POST | `/api/v1/auth/logout` | 吊销 refresh | 登录 |
| GET | `/api/v1/auth/captcha` | 登录数学验证码（图片或题面） | 公开 |
| GET | `/api/v1/auth/me` | 当前用户信息 | 登录 |
| POST | `/api/v1/auth/webauthn/register/begin` | passkey 注册开始 | 登录 |
| POST | `/api/v1/auth/webauthn/register/complete` | passkey 注册完成 | 登录 |
| POST | `/api/v1/auth/webauthn/login/begin` | passkey 登录开始 | 公开 |
| POST | `/api/v1/auth/webauthn/login/complete` | passkey 登录完成 | 公开 |
| GET/DELETE | `/api/v1/auth/webauthn/credentials[/{id}]` | passkey 列表/删除 | 登录 |
| **文章** | | | |
| GET | `/api/v1/articles?type=&page=` | 列表（可按 type 过滤） | 公开 |
| GET | `/api/v1/articles/recent?limit=` | 跨类型最新（主页推荐） | 公开 |
| GET | `/api/v1/articles/{type}/{slug}` | 详情（含 html） | 公开 |
| POST | `/api/v1/articles` | 新建 | admin+ |
| PUT | `/api/v1/articles/{type}/{slug}` | 编辑 | admin+/作者 |
| DELETE | `/api/v1/articles/{type}/{slug}` | 删除 | admin+/作者 |
| GET | `/api/v1/articles/{type}/{slug}/raw` | 原始内容（编辑用） | admin+/作者 |
| GET | `/api/v1/articles/{type}/{slug}/pdf` | Typst PDF 下载 | 公开 |
| **评论 / 行评论** | | | |
| GET | `/api/v1/articles/{type}/{slug}/comments` | 全文评论 | 公开 |
| POST | `/api/v1/articles/{type}/{slug}/comments` | 发表 | 登录 |
| GET | `/api/v1/articles/{type}/{slug}/line-comments` | 行评论（含分组） | 公开 |
| POST | `/api/v1/articles/{type}/{slug}/line-comments` | 发表行评论（≤20字） | 登录 |
| DELETE | `/api/v1/comments/{id}` | 删除评论 | 作者/admin+ |
| **评分** | | | |
| GET | `/api/v1/articles/{type}/{slug}/rating` | 汇总 + 当前用户分 | 公开 |
| POST | `/api/v1/articles/{type}/{slug}/rating` | 提交/更新（[-1,1]） | 登录 |
| DELETE | `/api/v1/articles/{type}/{slug}/rating` | 撤销 | 登录 |
| **标签** | | | |
| GET | `/api/v1/tags` | 全部标签 | 公开 |
| GET | `/api/v1/tags/{name}` | 标签下文章 | 公开 |
| POST/PUT/DELETE | `/api/v1/admin/tags...` | 标签管理 | admin+ |
| **搜索** | | | |
| GET | `/api/v1/search?q=&page=` | 全文搜索（MATCH AGAINST） | 公开 |
| **主页配置** | | | |
| GET | `/api/v1/site/home` | 子站点链接+推荐+通知聚合 | 公开 |
| GET/POST/PUT/DELETE | `/api/v1/admin/{subsites,featured,notifications}...` | 主页管理 | admin+ |
| **站点设置** | | | |
| GET | `/api/v1/site/settings` | 公开设置（标题/副标题/logo…） | 公开 |
| PUT | `/api/v1/admin/settings` | 改设置 | owner |
| **文件** | | | |
| GET | `/static/uploads/{file}` | 上传文件 | 公开 |
| POST | `/api/v1/admin/files` | 上传（multipart） | admin+ |
| GET/DELETE | `/api/v1/admin/files...` | 文件列表/删除 | admin+ |
| **用户管理** | | | |
| GET/POST | `/api/v1/admin/users` | 用户列表/新增 | owner |
| PUT/DELETE | `/api/v1/admin/users/{username}` | 角色/删除 | owner |
| POST | `/api/v1/admin/users/{username}/reset-password` | 重置密码 | owner |
| **个人** | | | |
| GET/PUT | `/api/v1/me/profile` | 资料 | 登录 |
| POST | `/api/v1/me/password` | 改密码 | 登录 |
| POST | `/api/v1/me/avatar` | 头像上传 | 登录 |
| GET | `/avatars/{file}` | 头像 | 公开 |
| **健康** | | | |
| GET | `/api/v1/health` | 含 DB 探活 | 公开 |

> 注：路径里的 `{type}` ∈ {md, wikidot, html, bbcode, typst}，对外 URL 仍按类型分前缀（`/md/{slug}` 等），用户感知不变；但后端一律查统一 `articles` 表。

### 3.4 渲染调度（render 包）

```go
// internal/render/render.go
func RenderHTML(articleType, content string) (template.HTML, error) {
    switch articleType {
    case "md":       return markdown.Render(content), nil
    case "wikidot":  return wikidot.Render(content), nil
    case "bbcode":   return bbcode.Render(content), nil
    case "html":     return template.HTML(content), nil   // 原样返回
    case "typst":    return typst.RenderHTML(content)      // 调 CLI + 缓存
    }
}
```
- MD/Wikidot/BBCode 的渲染结果 HTML 经服务端生成，前端只负责贴 DOM + 提供内容样式。
- Typst 的 HTML/PDF 编译走缓存表 `typst_cache`，命中即返回；未命中编译后回填。

### 3.5 Markdown / Wikidot / BBCode 解析器

- **Markdown**：Goldmark + extension(Table, Strikethrough) + TOC + 自写 admonition 预处理，对齐 Python 的 extra/fenced_code/toc/admonition/sane_lists。
- **Wikidot**：Python 版 18 个预编译正则分阶段替换，Go 版 **1:1 移植**（`regexp.MustCompile` 顺序一致）。用种子文章 `syntax-test` 做 HTML 比对回归。
- **BBCode**：同上，1:1 移植。

### 3.6 Typst

`os/exec.Command("typst","compile",...)` + 临时目录，逻辑照搬 `typst_parser.py`。CLI 路径检测（TYPST_PATH / which / Homebrew/cargo）照搬。

### 3.7 配置与启动

- 配置来源：环境变量（`DB_*`、`SESSION_SECRET`/`JWT_SECRET`、`APP_PORT`、`GIN_MODE`）+ `data/settings/{db.yaml,settings.yml}`。
- 启动流程：加载配置 → 连池（库不存在自动建）→ 建表（全新 schema）→ seed 默认 admin/settings → 注册路由 → `gin.Run()`。

---

## 4. 数据库方案（全面重构）

### 4.1 原则

全新库，建表即终态，无迁移逻辑。合并同构表，外键化关联。

### 4.2 Schema

```sql
-- 统一文章表（合并 5 张）
CREATE TABLE articles (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    type        ENUM('md','wikidot','html','bbcode','typst') NOT NULL,
    slug        VARCHAR(255) NOT NULL,
    title       VARCHAR(255) NOT NULL,
    content     LONGTEXT NOT NULL,
    author_id   INT DEFAULT NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uq_type_slug (type, slug),
    INDEX idx_type_created (type, created_at),
    INDEX idx_author (author_id),
    FULLTEXT INDEX ft_title_content (title, content)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 用户（avatar/bio 直接并入）
CREATE TABLE users (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    username      VARCHAR(64) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,           -- bcrypt
    nickname      VARCHAR(128) NOT NULL DEFAULT '',
    role          ENUM('user','admin','owner') NOT NULL DEFAULT 'user',
    avatar        VARCHAR(255) DEFAULT NULL,
    bio           VARCHAR(500) DEFAULT '',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 标签 + 关联
CREATE TABLE tags (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(64) UNIQUE NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE article_tags (
    article_id INT NOT NULL, tag_id INT NOT NULL,
    PRIMARY KEY (article_id, tag_id),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id)     REFERENCES tags(id)     ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 评论（全文 + 行评论合并，line_number 区分）
CREATE TABLE comments (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    article_id  INT NOT NULL,
    line_number INT DEFAULT NULL,                   -- NULL=全文；非空=行评论(≤20字)
    author_name VARCHAR(128) NOT NULL DEFAULT '匿名',
    content     VARCHAR(500) NOT NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    INDEX idx_article_line (article_id, line_number)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 评分（[-1.00, 1.00]，DECIMAL(4,2)，应用层 clamp）
CREATE TABLE ratings (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    article_id  INT NOT NULL,
    author_name VARCHAR(128) NOT NULL,
    score       DECIMAL(4,2) NOT NULL,              -- -1.00 ~ 1.00
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uq_user_article (article_id, author_name),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    INDEX idx_author_name (author_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 通行密钥（user_id 外键，credential_id 用 VARBINARY）
CREATE TABLE webauthn_credentials (
    id            INT AUTO_INCREMENT PRIMARY KEY,
    user_id       INT NOT NULL,
    credential_id VARBINARY(1024) NOT NULL UNIQUE,
    public_key    BLOB NOT NULL,
    sign_count    BIGINT DEFAULT 0,
    transports    VARCHAR(255) DEFAULT '',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 主页配置
CREATE TABLE subsite_links (
    id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(255) NOT NULL,
    url VARCHAR(1024) NOT NULL, description VARCHAR(512) DEFAULT '',
    sort_order INT NOT NULL DEFAULT 0, INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE featured_articles (
    id INT AUTO_INCREMENT PRIMARY KEY, article_id INT NOT NULL,
    sort_order INT NOT NULL DEFAULT 0, UNIQUE KEY uq_featured (article_id),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,
    INDEX idx_sort (sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 通知
CREATE TABLE notifications (
    id INT AUTO_INCREMENT PRIMARY KEY, title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL, is_important TINYINT(1) NOT NULL DEFAULT 0,
    is_active TINYINT(1) NOT NULL DEFAULT 1,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_active_important (is_active, is_important)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 上传文件
CREATE TABLE static_files (
    id INT AUTO_INCREMENT PRIMARY KEY, filename VARCHAR(255) UNIQUE NOT NULL,
    original_name VARCHAR(255) NOT NULL, file_path VARCHAR(512) NOT NULL,
    file_size BIGINT NOT NULL DEFAULT 0,
    mime_type VARCHAR(128) DEFAULT 'application/octet-stream',
    uploaded_by INT DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_uploaded_by (uploaded_by)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Typst 辅助文件 + 缓存
CREATE TABLE typst_files (
    id INT AUTO_INCREMENT PRIMARY KEY, article_id INT NOT NULL,
    filename VARCHAR(255) NOT NULL, content LONGTEXT NOT NULL,
    UNIQUE KEY uq_article_file (article_id, filename),
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE typst_cache (
    id INT AUTO_INCREMENT PRIMARY KEY, article_id INT NOT NULL UNIQUE,
    html_content LONGTEXT NOT NULL, pdf_content LONGBLOB NOT NULL,
    dependencies TEXT DEFAULT NULL,
    compiled_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**15 → 12 张表**，删除 UNION 视图与全部 `_migrate_*` 函数。搜索从 5 路 UNION 变单表 `MATCH ... AGAINST`。

### 4.3 密码 → bcrypt

```go
import "golang.org/x/crypto/bcrypt"
func Hash(p string) (string, error) { b,_ := bcrypt.GenerateFromPassword([]byte(p), 12); return string(b), nil }
func Verify(h, p string) bool       { return bcrypt.CompareHashAndPassword([]byte(h),[]byte(p)) == nil }
```
密码强度校验（≥8、大小写+数字）照搬 `password.py`。启动时仅当 `users` 表为空才 seed `admin/admin123`(owner)。

### 4.4 站点设置

全新生成默认 `settings.yml`（结构同 PyKYCH `DEFAULT_SETTINGS`）。**丢弃现有设置**（用户已确认）。

---

## 5. 认证方案（JWT）

### 5.1 为什么 JWT 而非 session cookie

前后端分离后，JWT 是无状态、易跨域、前后端都好处理的方案。PyKYCH 的 session cookie + CSRF 方案是为同源 SSR 设计的，分离架构下不再契合。

### 5.2 双 token

- **access token**：短期（15 min），放 `Authorization: Bearer <token>`。载荷含 `user_id / username / role`。
- **refresh token**：长期（7 天），`HttpOnly + Secure + SameSite=Strict` cookie（前端 JS 读不到，防 XSS 盗取）。后端存 refresh 的 jti 到 DB 或内存，支持主动吊销（登出）。
- 前端用 axios/fetch 拦截器：access 过期(401)→用 refresh 自动换新→重放原请求。

### 5.3 安全对齐

- 登录速率限制：每「用户名+IP」5 次/分钟，连续 10 次失败锁 15 分钟（照搬 `rate_limit.py`）。
- 数学验证码防机器人（照搬，题面经 API 返回）。
- 恒定时间比较（`subtle.ConstantTimeCompare`）用于验证码、refresh 校验。
- 密码强度校验。
- 开放重定向防护（登录后跳转仅允许站内）。
- **CSRF**：JWT + SameSite=Strict cookie，**天然免疫 CSRF**，不再需要 CSRF token。

### 5.4 WebAuthn / passkey

用 `github.com/go-webauthn/webauthn`（替换 Python 自写 CBOR/验签）。首版可先禁用 passkey 登录、仅保证账密链路；passkey 列为 v1.1。

### 5.5 权限

Gin 中间件：`RequireAuth` / `RequireAdmin`(role∈{admin,owner}) / `RequireOwner`。401/403 返回统一 error。

---

## 6. 前端设计（Next.js）

### 6.1 技术栈

- Next.js (App Router) + TypeScript。
- 数据获取：SSR/SSG（`fetch` 调后端，Next 缓存）+ 客户端交互（评分、行评论用客户端组件 + SWR/React Query）。
- 样式：保留 PyKYCH 的设计语言——把 `base.html` 里 1011 行内联 CSS **提取为全局样式 + CSS Modules / Tailwind**，黑暗模式（`data-theme`）逻辑保留。
- 状态：React Context（当前用户/JWT）+ 轻量客户端缓存（SWR）。
- 组件库：轻量自写或 shadcn/ui，对齐原视觉。
- 富文本/编辑器：文章编辑用 `@uiw/react-md-editor` 或简单 textarea（按类型给不同编辑器）。

### 6.2 页面映射（32 模板 → Next 页面）

| PyKYCH 模板 | Next.js 路由 |
|-------------|-------------|
| home.html | `/` |
| page.html (关于) | `/about` |
| md_list / md_detail | `/md`, `/md/[slug]` |
| wikidot_* | `/wikidot`, `/wikidot/[slug]` |
| html_list / html_detail | `/html`, `/html/[slug]` |
| bbcode_* | `/bbcode`, `/bbcode/[slug]` |
| typst_* | `/typst`, `/typst/[slug]`（PDF 链接指向后端 `/api/v1/.../pdf`） |
| labels / label_detail | `/labels`, `/labels/[name]` |
| search | `/search` |
| login | `/login` |
| admin_* (14 个) | `/admin/*`（管理后台 SPA 子路由：dashboard/articles/tags/files/users/settings/notifications/profile/plugins/themes） |

对外 URL 与原项目一致，用户访问路径不变。

### 6.3 内容渲染对接

- 文章详情页拿到 `{html: "..."}` 后 `<div dangerouslySetInnerHTML={{__html: data.html}} />`。
- **内容样式**（代码高亮、admonition、表格、collapsible、行评论侧栏锚点）由前端提供 CSS，对应 PyKYCH 模板里的内联样式。
- 行评论交互：客户端组件按 `line_number` 锚定到正文行，点击展开输入框，调 `/line-comments` API。

### 6.4 管理后台

- `/admin/*` 为受保护路由（前端检查 JWT/role，无权限跳登录）。
- 文章 CRUD 用统一表单（按 type 切换渲染预览）。新增/编辑调 `POST/PUT /api/v1/articles`。
- 文件上传、用户管理、设置、通知、主页管理各对应 admin API。

---

## 7. 部署

### 7.1 推荐形态：单二进制

- Next.js `next build && next export` → `web/out/` 静态文件。
- Go 用 `go:embed web/out` 打进二进制，Gin `router.Static` 或 `NoRoute` 兜底返回 `index.html`（SPA fallback）。
- 一个 `gokych` 二进制 + MySQL，单端口 8000。运维最简。

### 7.2 分离部署（可选）

- 前端独立（Vercel / Node / Nginx），后端开 CORS。
- 适合前端频繁迭代、灰度发布的场景。

### 7.3 Docker

```dockerfile
# 阶段1：前端构建
FROM node:20-alpine AS web
WORKDIR /web
COPY web-frontend/ .           # Next.js 工程
RUN npm ci && npm run build && npx next export -o out

# 阶段2：Go 构建（embed 前端产物）
FROM golang:1.26-alpine AS go
WORKDIR /src
COPY go.* ./
RUN go mod download
COPY . .
COPY --from=web /web/out ./web/out
RUN CGO_ENABLED=0 go build -o /out/gokych ./cmd/gokych

# 阶段3：运行（含 typst）
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
# 安装 typst（同 PyKYCH 方式）
COPY --from=go /out/gokych /app/gokych
COPY configs/ /app/configs/
WORKDIR /app
EXPOSE 8000
ENTRYPOINT ["/app/gokych"]
```
docker-compose 复用 PyKYCH 的 MySQL 8 服务（3307、utf8mb4、healthcheck）。

---

## 8. Go 依赖清单（go.mod）

```
github.com/gin-gonic/gin               # Web
github.com/go-sql-driver/mysql         # MySQL 驱动
github.com/golang-jwt/jwt/v5           # JWT
golang.org/x/crypto/bcrypt             # 密码
github.com/yuin/goldmark + extensions  # Markdown
github.com/go-webauthn/webauthn        # WebAuthn (v1.1)
golang.org/x/time/rate                 # 限流
gopkg.in/yaml.v3                       # YAML
github.com/google/uuid                 # UUID
```

前端（package.json）：`next react react-dom`，`swr`，`axios`，可选 `shadcn/ui`/`tailwindcss`，`@uiw/react-md-editor`。

---

## 9. 验收标准（Definition of Done）

1. **全新数据库**：空 MySQL 首启自动建 12 表，seed admin + 默认设置。
2. **认证**：bcrypt；admin/admin123 登录拿 JWT；access/refresh 流转正常；强度校验、限流、验证码生效。
3. **功能对齐**：5 类文章浏览/CRUD、评论/行评论、评分、标签、搜索、主页、通知、管理后台——浏览器逐功能对比 PyKYCH，交互流程一致。
4. **渲染一致**：MD/Wikidot/BBCode 同源文本渲染 HTML 视觉与 PyKYCH 一致。
5. **安全对齐**：JWT + SameSite cookie（免 CSRF）、限流、恒定时间比较、路径遍历防护、Secure cookie。
6. **部署**：`docker compose up` 起完整服务（前端+后端+MySQL），首页与后台可用。
7. **暂缓项明确**：插件/主题/外部抓取在代码标 TODO，不阻塞核心。

---

## 10. 实施顺序与里程碑

```
M0  后端脚手架    go.mod、config、DB 池、schema(12表)、seed、health
M1  认证          bcrypt + JWT(access/refresh) + 限流 + 验证码 + 登录/登出/me
M2  文章 API      统一 CRUD + Markdown 渲染 + 列表/详情/raw
M3  其余渲染      Wikidot/BBCode/HTML 解析器 + Typst + PDF
M4  交互 API      评论(含行评论)、评分、标签、搜索
M5  管理 API      admin 全套（文章/标签/文件/用户/设置/通知/主页）
─────────────────────────────────────────────────────  后端完成
M6  前端脚手架    Next.js 工程、全局样式(从 base.html 提取)、JWT 拦截、布局
M7  前台页面      首页/关于/5 类文章列表+详情/标签/搜索/登录
M8  交互组件      评论区、行评论、评分（客户端组件 + SWR）
M9  管理后台      /admin/* 全部页面
M10 部署收尾      embed/分离、Dockerfile、compose、README、安全/功能回归
```
后端(M0–M5)与前端(M6–M9)可一定程度并行：M2 出文章详情 API 后前端 M7 即可起步。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| Wikidot/BBCode 正则移植出错 | 渲染异常 | 种子文章 HTML 比对回归 |
| 内容样式迁移（1011 行 CSS） | 视觉不一致 | 系统性提取 + 逐页对比截图 |
| 行评论「贴在正文行侧」交互复杂 | 体验降级 | 先做基础行评论列表，侧栏锚点迭代 |
| JWT refresh 吊销/续期实现 | 登录态异常 | refresh 存 DB jti，登出/改密吊销；access 短期降风险 |
| WebAuthn 协议差异 | passkey 不可用 | 首版禁用，v1.1 |
| 单二进制 embed 前端的 SPA 路由 | 刷新 404 | `NoRoute` 兜底返回 index.html |
| 暂缓插件/主题/外部抓取 | 功能缺口 | 文档明确，核心不受影响 |

---

## 12. 不做什么（首版明确排除）

- ❌ 插件系统、主题系统（模板覆盖）——留接口占位。
- ❌ 外部 HTML 抓取——彻底移除（代码本已删）；HTML 类型保留为纯手写。
- ❌ 数据迁移 / 旧库兼容 / PBKDF2 兼容——全新库 + bcrypt。
- ❌ PyKYCH 现有 fetch 契约——API 全新设计。
- ❌ 改变对外 URL 结构（`/md/{slug}` 等）——用户感知不变。
- ❌ 替换 typst（仍调 CLI）。

这些是有意识的范围控制，目标是让核心网站以更干净的分离架构尽快上线。
