# 跨越晨昏 — PyKYCH → GoKYCH 重写方案

> 源项目：`../PyKYCH`（Python + LiHiL + Jinja2 + aiomysql，约 11.9k 行）
> 目标项目：`GoKYCH`（Go + Gin + html/template + database/sql）
> 本文是**实施前的方案设计**，不是已完成记录。

---

## 0. TL;DR

将 PyKYCH 的全部网站功能用 **Go + Gin** 重写。**核心业务（文章/评论/评分/标签/搜索/管理后台/认证）100% 对齐**；**插件系统与主题系统首版暂缓**（仅保留文件结构与占位接口）。数据库 schema 完全复用，保证现有数据无缝迁移。

- 模板：**逐个转写为 Go `html/template`**（32 个，约 6700 行）。
- Markdown：**Goldmark + 扩展**（对齐 Python 的 extra/fenced_code/toc/admonition/sane_lists）。
- 代码组织：**Go 标准布局**（`cmd/` + `internal/`）。
- 扩展系统：**全部暂缓**，首版只跑通核心。

---

## 1. 现状摸底（来自 PyKYCH）

### 1.1 规模

| 维度 | 数量 |
|------|------|
| Python 源文件 | 43 个（不含 `__pycache__`） |
| 代码行数 | 约 11,892 行 |
| HTTP 端点 | 约 217 个（其中 admin 占 65） |
| 数据库表 | 15 张 + 1 视图 `VW_ALL_ARTICLES` |
| Jinja2 模板 | 32 个，约 6,706 行 |
| 静态资源 | 仅 `img/`（logo/favicon），**CSS 全部内联在 `base.html`（1011 行）**，无独立 JS/CSS 文件 |

### 1.2 模块结构（PyKYCH `src/pykych/`）

```
core/        基础设施  db(连接池) schema(建表迁移) settings(YAML) site_settings templates(jinja)
auth/        认证      password(PBKDF2) session(会话/CSRF) user profile rate_limit webauthn
content/     内容      articles(统一CRUD) tags comments ratings files notifications
             parsers/  wikidot bbcode typst_parser(调 typst CLI)
routes/      路由      admin(1553行,65端点) auth api md wikidot html bbcode typst comments labels search
plugins_sys/ 插件      动态 import .py 包，ON_STARTUP/ON_SHUTDOWN 钩子
themes_sys/  主题      模板覆盖 + theme.css
main.py      入口      生命周期、首页、健康检查、静态文件/avatar 路由
```

### 1.3 关键技术依赖（Python）

| 能力 | Python 实现 | Go 对应方案 |
|------|------------|------------|
| Web 框架 | LiHiL (ASGI) | **Gin** |
| 模板 | Jinja2 | **html/template**（手动转写） |
| 数据库 | aiomysql 异步连接池 | **database/sql + go-sql-driver/mysql** |
| Markdown | python-markdown + 5 扩展 | **Goldmark + 扩展** |
| Wikidot 解析 | 自写 18 个正则 | Go 重写正则解析器 |
| BBCode 解析 | 自写（基于 `re`） | Go 重写 |
| Typst 渲染 | `subprocess` 调 `typst` CLI | `os/exec` 调 `typst` CLI（逻辑直接搬） |
| 密码哈希 | PBKDF2-SHA256, 60 万次, NFC, `$pbkdf2$iters$salt$hash` 格式 | Go `crypto/pbkdf2` + `golang.org/x/text/unicode/norm`（**必须同格式同参数**） |
| 会话 | Starlette SessionMiddleware（签名 Cookie） | **gorilla/sessions**（或自写 HMAC 签名 Cookie） |
| CSRF | itsdangerous 签名 token | **gorilla/csrf** 或自写 |
| WebAuthn | cryptography + 自写 CBOR 解码 | **go-webauthn/webauthn**（替换自写 CBOR/验签） |
| 速率限制 | 进程内 dict + 锁 | **ulule/limiter** 或 `golang.org/x/time/rate` |
| YAML | PyYAML | **yaml.v3** |
| 外部 HTML 抓取 | aiohttp | **net/http** |

---

## 2. 技术选型（已确认）

### 2.1 模板引擎 → Go html/template

- **决定**：逐个把 32 个 Jinja2 模板**重写**为 `html/template`。
- 理由：用户明确选择；长期纯 Go 技术栈，避免引入第三方 Jinja 兼容层的维护负担。
- 迁移映射规则：

  | Jinja2 | Go html/template |
  |--------|------------------|
  | `{% extends "base.html" %}` + `{% block x %}` | 定义一组 `*.tmpl` 用 `{{define "base"}}…{{end}}` + `{{template "content" .}}`；或 base.tmpl + 各页 tmpl |
  | `{% include "_x.html" %}` | `{{template "_x" .}}` |
  | `{% macro f(x) %}` | `{{define "f"}}…{{end}}` + 显式传参 |
  | `{{ x \| default('y') }}` | `{{ if .x }}{{ .x }}{{ else }}y{{ end }}` 或 FuncMap `default` |
  | `{{ x \| safe }}` | `{{ template "html" .x }}` 或用 `template.HTML` 类型（**默认 html/template 已转义，safe 即「不转义」，需用 template.HTML**） |
  | `{{ x \| e }}` | 默认行为（自动转义），删除即可 |
  | `{{ x \| length }}` | `{{ len .x }}` |
  | `{% if x == 'y' %}` | `{{ if eq .x "y" }}` |
  | `{% for i in xs %}` | `{{ range .xs }}` |
  | `{% set y = z %}` | 在 handler 里算好传入，或用 FuncMap；少数复杂处可在模板用 `{{$y := .z}}` |
  | `site_logo()` 等全局函数 | 注入到 `template.FuncMap`，或作为 `ViewData` 字段预渲染 |

- **风险点**：`base.html` 1011 行含大量内联 CSS + JS，是最大单文件。需**整块复制 CSS/JS 原文**（不动），只改 `{{ }}` 控制流。建议第一周先攻克 `base.html` + `home.html` 跑通首页。
- 上下文数据结构：定义统一的 `ViewData` 结构体（标题、当前用户、CSRF token、站点设置等），所有 handler 传入。

### 2.2 Markdown → Goldmark + 扩展

- **决定**：`github.com/yuin/goldmark`。
- 对齐 Python 扩展：
  - `extra`（表格/删除线/定义列表）→ `goldmark/extension/table` + `strikethrough`
  - `fenced_code` → goldmark 默认支持
  - `toc` → `github.com/abhinav/goldmark-toc` 或自写 heading 抽取
  - `admonition`（`!!! note`）→ **自写扩展**（goldmark 插件机制）或预处理替换为 `<div class="admonition">`
  - `sane_lists` → goldmark 默认行为已较合理
- 代码块语言前缀：Python 用 `language-`，goldmark 默认 `language-`，**一致**。
- 输出 `html5` → goldmark 默认即 HTML5。

### 2.3 扩展系统 → 全部暂缓

- **插件系统**（`plugins_sys`）：Python 版动态 `import` `.py` 包，Go 无法 import Python。**首版不实现**，仅保留 `data/plugins/` 目录与一个空接口 `PluginHooks`（`OnStartup/OnShutdown`）作为占位，供后续用 Go plugin 或 webhook 机制实现。
- **主题系统**（`themes_sys`）：**首版不实现**模板覆盖与 `theme.css` 路由。`base.html` 内联 CSS 保持原样。`/theme.css` 路由返回空 CSS 或静态占位。`data/themes/` 目录保留。
- 两个系统在 README 与代码中标注 `// TODO(v2): plugin/theme system`。

### 2.4 代码组织 → Go 标准布局

```
GoKYCH/
├── cmd/
│   └── gokych/
│       └── main.go              # 入口：加载配置、初始化 DB、注册路由、启动 Gin
├── internal/
│   ├── config/                  # 配置加载（env + data/settings/*.yaml）
│   │   └── config.go
│   ├── core/
│   │   ├── db/                  # database/sql 连接池 + 自动建库
│   │   │   └── db.go
│   │   ├── schema/              # 15 张表 + 视图 + 迁移（CREATE TABLE IF NOT EXISTS）
│   │   │   └── schema.go
│   │   ├── settings/            # settings.yml 读写（站点设置）
│   │   │   └── settings.go
│   │   └── sitesettings/        # 子站点链接、推荐文章（site_settings.py）
│   │       └── sitesettings.go
│   ├── auth/
│   │   ├── password/            # PBKDF2-SHA256（与 Python 同格式同参数）
│   │   ├── session/             # 会话（gorilla/sessions）+ get_current_user/login/logout
│   │   ├── csrf/                # CSRF token 生成/校验
│   │   ├── ratelimit/           # 登录速率限制
│   │   ├── user/                # 用户 CRUD + 角色判断
│   │   ├── profile/             # 用户资料/头像
│   │   └── webauthn/            # 通行密钥（go-webauthn 库）
│   ├── content/
│   │   ├── articles/            # 统一文章 CRUD（md/wikidot/html/bbcode/typst）
│   │   ├── tags/                # 标签管理
│   │   ├── comments/            # 评论 + 行评论
│   │   ├── ratings/             # 评分
│   │   ├── files/               # 文件上传管理
│   │   ├── notifications/       # 通知
│   │   └── parsers/
│   │       ├── markdown/        # Goldmark 封装
│   │       ├── wikidot/         # Wikidot→HTML（重写 18 正则）
│   │       └── bbcode/          # BBCode→HTML（重写）
│   ├── typst/                   # typst CLI 调用 + 缓存表
│   │   └── typst.go
│   ├── routes/                  # Gin handlers（与 Python routes/ 一一对应）
│   │   ├── home.go              # / /about /health
│   │   ├── md.go wikidot.go html_route.go bbcode.go typst_route.go
│   │   ├── labels.go comments.go search.go
│   │   ├── auth.go              # /auth/*
│   │   ├── api.go               # /api/me /api/line-comments /api/ratings
│   │   ├── admin.go             # /admin/*（最大，65 端点，拆分子文件）
│   │   └── static.go            # /static/uploads /static/img /avatars /theme.css
│   └── render/                  # html/template 引擎封装 + FuncMap + ViewData
│       └── render.go
├── web/
│   ├── templates/               # 转写后的 *.html.tmpl（32 个）
│   └── static/                  # img/（logo/favicon，直接复制）
├── data/                        # 运行时数据（gitignore，结构同 PyKYCH）
│   ├── settings/{db.yaml, settings.yml}
│   ├── avatars/
│   ├── plugins/                 # 占位
│   ├── themes/                  # 占位
│   └── typst/
├── configs/
│   ├── db.yaml.example          # 复制自 PyKYCH
│   └── .env.example
├── go.mod
├── go.sum
├── Dockerfile                   # Go 版（多阶段构建，含 typst CLI）
├── docker-compose.yml           # 复用 PyKYCH 的 MySQL 服务
└── README.md
```

---

## 3. 数据库方案

### 3.1 Schema 完全复用

**不改表结构**，直接搬运 PyKYCH 的 15 张表 + 1 视图（见 `core/schema.py` 的 `*_TABLE_SQL` 常量）。这样现有生产数据可无缝接入 GoKYCH。

15 张表：`articles pages html_pages bbcode_pages typst_pages users tags article_tags comments line_comments ratings webauthn_credentials subsite_links featured_articles notifications static_files typst_files typst_cache`。
视图：`VW_ALL_ARTICLES`（5 表 UNION ALL）。

### 3.2 迁移逻辑移植

Python 的 `init_tables()` 在启动时执行：建表 → `_safe_add_column`（幂等加列）→ 角色迁移 → ENUM 扩展 → 索引 → FULLTEXT → 视图。**Go 版按顺序实现相同语句**，全部是 `CREATE TABLE IF NOT EXISTS` / `ALTER TABLE ... ADD ...` 包在 `try/ignore` 里，幂等安全。

- FULLTEXT 索引（OPT-007）：Go 版用 `context.WithTimeout(30s)` 包裹，超时跳过，与 Python 一致。
- 环境变量 `PYKYCH_SKIP_FULLTEXT` → Go 版沿用同名（或新增 `GOKYCH_SKIP_FULLTEXT`，同时读两个）。

### 3.3 密码哈希**必须兼容**

PyKYCH 存储格式：`$pbkdf2$600000$<salt_hex>$<hash_hex>`，PBKDF2-HMAC-SHA256，60 万次，盐 32 字节，**密码先做 Unicode NFC 规范化再 UTF-8 编码**。

Go 实现：
```go
// internal/auth/password/password.go
import "golang.org/x/crypto/pbkdf2"
import "golang.org/x/text/unicode/norm"
import "crypto/sha256"

func HashPassword(plain string) string {
    pw := norm.NFC.Bytes([]byte(plain))
    salt := make([]byte, 32); rand.Read(salt)
    key := pbkdf2.Key(pw, salt, 600000, 32, sha256.New)
    return fmt.Sprintf("$pbkdf2$600000$%x$%x", salt, key)
}
func VerifyPassword(plain, stored string) bool { /* 解析 + pbkdf2.Key + subtle.ConstantTimeCompare */ }
```
**验证标准**：用 PyKYCH 生成的 admin 哈希（admin123）在 GoKYCH 登录必须通过。这是验收的硬性测试项。

### 3.4 默认管理员

启动时 `seed_admin("admin","admin123")` 逻辑照搬（仅当 username 不存在时插入 owner）。

---

## 4. 认证与会话

### 4.1 会话

- 用 **gorilla/sessions**（cookie store，HMAC 签名），密钥来自环境变量 `SESSION_SECRET`（沿用 `.env.example` 已有项）。
- 会话键：`user`(string) / `login_time` / `last_activity` / `csrf_token` / `captcha_answer`。
- 会话过期：24h 空闲，每次请求更新 `last_activity`（与 Python `SESSION_MAX_AGE` 一致）。
- Secure Cookie：生产环境（非 localhost）启用 `Secure`，`HttpOnly=true`，`SameSite=Lax`。

### 4.2 CSRF

- 登录时生成 64 字符 hex token 存入 session；所有 POST 校验 `csrf_token` 字段。
- 用 `subtle.ConstantTimeCompare`（恒定时间），对齐 Python 的 `hmac.compare_digest`。
- 中间件：给 `/admin/*`、`/auth/login`(POST)、评论/评分 API 统一加 CSRF 校验。

### 4.3 速率限制

- 登录：每「用户名+IP」5 次/分钟，连续 10 次失败锁 15 分钟（进程内 map + sync.Mutex）。对齐 `auth/rate_limit.py`。
- `check-passkey`：每 IP 每分钟 10 次。
- Go：`golang.org/x/time/rate` 或自写滑动窗口。

### 4.4 WebAuthn / 通行密钥

- Python 自写 CBOR 解码 + cryptography 验签（629 行）。Go 版**不重写**，直接用 **`github.com/go-webauthn/webauthn`** 库。
- 凭据存储表 `webauthn_credentials` 复用；需写一层适配把库的 `Credential` 对象映射到表结构。
- RP 配置（`rp_id`/`origin`）从请求头 `X-Forwarded-Host`/`X-Forwarded-Proto` 推导，逻辑照搬 `_get_rp_config`。
- **风险**：库的协议细节与 Python 自实现可能存在 sign_count 等差异；首版可先**禁用 passkey 登录**（返回提示），保证密码登录链路先通，passkey 作为 v1.1。

### 4.5 权限中间件

三个 Gin 中间件（对齐 `require_login/require_admin/require_owner`）：
- `RequireLogin()`：未登录 303 跳 `/auth/login?next=<urlencode(path)>`
- `RequireAdmin()`：再校验 role ∈ {admin, owner}
- `RequireOwner()`：再校验 role == owner

---

## 5. 内容渲染与解析器

### 5.1 Markdown（Goldmark）

封装 `internal/content/parsers/markdown/render.go`：
```go
var md = goldmark.New(
    goldmark.WithExtensions(extension.Table, extension.Strikethrough, extension.Typographer),
    goldmark.WithParserOptions(parser.WithAutoHeadingID()),
)
func Render(text string) template.HTML { /* md.Convert → template.HTML */ }
```
admonition（`!!! note`）作为**预处理**：在渲染前用正则把 `!!! note\n  xxx` 转成兼容的写法，或写 goldmark ast 插件。

### 5.2 Wikidot 解析器

Python 版 `wikidot.py` 用 18 个预编译正则，分阶段替换（代码块占位 → 块元素 → 行内 → 还原）。**Go 版 1:1 移植**：`regexp.MustCompile` 一一对应，处理顺序一致。这是确定性工作，但需仔细对照逐行翻译，并**用 PyKYCH 的种子文章 `syntax-test` 做回归测试**（渲染结果 HTML 比对）。

### 5.3 BBCode 解析器

同上，1:1 移植正则替换逻辑。

### 5.4 Typst

`typst_parser.py` 做的事：检测 typst CLI 路径 → 在临时目录写 `.typ` + 关联文件 → `subprocess` 调 `typst compile` → 缓存到 `typst_cache` 表。**Go 版直接搬**：`os/exec.Command("typst", "compile", ...)` + 临时目录。CLI 检测路径（Homebrew / cargo 等）照搬。

---

## 6. 路由清单（217 端点，分批迁移）

按优先级分 4 批，每批是一个可独立验证的里程碑：

| 批次 | 范围 | 端点数 | 验证点 |
|------|------|--------|--------|
| **P0 基座** | 配置/DB/schema/会话/密码 + 首页/健康检查/静态资源 | ~10 | 服务能起、首页能开、admin 能登录 |
| **P1 阅读** | md/wikidot/html/bbcode/typst 列表+详情、labels、search | ~20 | 5 种文章都能浏览、搜索可用 |
| **P2 交互** | comments/line-comments/ratings API、用户资料/头像 | ~15 | 评论、评分、行评论可用 |
| **P3 管理后台** | `/admin/*` 全部 65 端点（文章CRUD/标签/文件/用户/设置/通知/主页管理） | 65 | 后台功能全对齐 |

`/auth/*`（10 端点，含 WebAuthn）与 `/api/*`（7 端点）随 P0/P2 推进。

每个 handler 的迁移模式高度一致（见 `routes/md.py` 模板）：
1. `get_current_user` → 中间件注入 `*User` 到 `c.Set`
2. 调 content 层方法（已迁移的 articles/tags/comments/ratings）
3. 组装 `ViewData`，`render.HTML(c, "xxx_list.html", data)`

---

## 7. 静态资源与文件服务

- `web/static/img/`：直接复制 PyKYCH 的 logo/favicon（3 个文件）。
- **CSS/JS 全部内联在模板里**（无独立文件），迁移时整块保留。
- 运行时路由（与 Python 一致）：
  - `/static/uploads/{file}` → 上传文件
  - `/static/img/{file}` → 站点图片
  - `/avatars/{file}` + `/static/avatars/{file}`（兼容旧路径）→ 头像
  - `/theme.css` → 主题 CSS（首版返回空或占位）
- **路径遍历防护**：`_safe_resolve` 逻辑用 `filepath.Clean` + `strings.HasPrefix` 检查在 base 目录内，照搬。

---

## 8. 部署

### 8.1 Docker（多阶段构建）

```dockerfile
# 阶段1：构建
FROM golang:1.26-alpine AS builder
WORKDIR /src
COPY go.* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/gokych ./cmd/gokych

# 阶段2：运行（含 typst CLI）
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
# 安装 typst（同 PyKYCH Dockerfile 的方式，下载预编译二进制）
COPY --from=builder /out/gokych /app/gokych
COPY web/ /app/web/
COPY configs/ /app/configs/
WORKDIR /app
EXPOSE 8000
ENTRYPOINT ["/app/gokych"]
```

### 8.2 docker-compose

复用 PyKYCH 的 MySQL 8.0 服务定义（端口 3307、utf8mb4、健康检查），`app` 服务换成 Go 镜像。环境变量沿用 `DB_HOST/DB_USER/DB_PASSWORD/DB_NAME`，新增 `SESSION_SECRET`。

### 8.3 生产部署

对应 PyKYCH README 的「Ubuntu + Nginx」流程，systemd unit 改为运行 Go 二进制（无需 venv/uvicorn），Nginx 反代 `127.0.0.1:8000`。这部分在 GoKYCH README 里重写。

---

## 9. Go 依赖清单（go.mod）

```
github.com/gin-gonic/gin                # Web 框架
github.com/go-sql-driver/mysql          # MySQL 驱动
github.com/gorilla/sessions             # 会话（cookie store）
gopkg.in/yaml.v3                        # YAML 配置
github.com/yuin/goldmark                # Markdown
github.com/yuin/goldmark-* (extensions) # 表格/删除线/TOC
golang.org/x/crypto/pbkdf2             # 密码哈希
golang.org/x/text/unicode/norm         # NFC 规范化
github.com/go-webauthn/webauthn        # WebAuthn（v1.1）
golang.org/x/time/rate                 # 速率限制
github.com/google/uuid                 # UUID（文件名等）
```

**所有依赖均为知名稳定库**，无冷门/停止维护风险。

---

## 10. 验收标准（Definition of Done）

1. **数据库兼容**：GoKYCH 指向 PyKYCH 同一数据库，能读取所有现有文章/评论/用户，**不执行任何破坏性迁移**。
2. **密码兼容**：PyKYCH 创建的用户能在 GoKYCH 登录（含 admin/admin123）。
3. **功能对齐**：P0–P3 全部端点行为与 PyKYCH 一致（用同一份数据，浏览器逐页对比）。
4. **渲染一致**：Markdown/Wikidot/BBCode 对同一源文本渲染出的 HTML 与 PyKYCH 视觉一致（允许无关紧要的空白差异）。
5. **安全对齐**：CSRF、速率限制、恒定时间比较、路径遍历防护、Secure Cookie 全部实现。
6. **部署可行**：`docker compose up` 能起一套完整服务（Go app + MySQL），首页与后台可用。
7. **暂缓项明确**：插件系统、主题系统在 README/代码中标注 TODO，不阻塞核心使用。

---

## 11. 实施顺序与里程碑

```
M0  脚手架        go.mod、cmd/main.go、config、DB 连接池、schema 建表迁移、seed_admin
                   → 能连库、建表、插入 admin                    (P0 一半)
M1  认证基座      password(PBKDF2) + session + csrf + ratelimit + 登录/登出
                   → admin/admin123 能登录                        (P0 完成)
M2  模板基座      render 包 + base.html 转写 + home.html
                   → 首页可见                                    (P0 完成)
M3  文章阅读      md 列表/详情 + Markdown 渲染
                   → 第一类文章全链路通                          (P1 开始)
M4  其余文章      wikidot/html/bbcode/typst + 对应解析器 + labels + search
                   → 5 类文章全通、搜索可用                      (P1 完成)
M5  交互          comments/line-comments/ratings API + profile/avatar
                   → 评论评分可用                                (P2 完成)
M6  管理后台      admin 65 端点（文章CRUD/标签/文件/用户/设置/通知/主页）
                   → 后台全功能                                  (P3 完成)
M7  部署与收尾    Dockerfile/compose、README、安全复核、兼容性回归
                   → 可交付                                      (Done)
```

每个里程碑结束都产出一个**可运行、可验证**的版本。

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| Wikidot/BBCode 正则移植出错 | 文章渲染异常 | 用 PyKYCH 种子文章 + 真实数据做 HTML 比对测试 |
| `base.html`(1011行) 转写工作量大 | 阻塞 M2 | 优先复刻 CSS/JS 原文，只改控制流；先跑通再优化 |
| WebAuthn 协议细节差异 | passkey 不可用 | 首版禁用 passkey 登录，密码链路先稳；passkey 进 v1.1 |
| FULLTEXT 索引大表超时 | 启动慢 | 沿用 30s 超时 + 环境变量跳过 |
| Jinja→Go 模板的 macro/include 语义差异 | 模板复用受影响 | 用 `{{define}}`/`{{template}}` 等价改写，复杂宏转 FuncMap |
| 暂缓插件/主题 | 功能缺口 | 文档明确告知，核心不受影响；预留接口便于后续 |

---

## 13. 不做什么（首版明确排除）

- ❌ 插件系统（`plugins_sys`）的动态加载与钩子执行——只留接口占位。
- ❌ 主题系统（`themes_sys`）的模板覆盖——`base.html` 内联样式保持单一主题。
- ❌ 重写数据库 schema——完全复用。
- ❌ 改变 URL 结构、会话语义、密码格式——保持兼容。
- ❌ 替换 typst（仍调外部 CLI）。

这些是**有意识的范围控制**，目的是让核心网站尽快在 Go 上跑起来。
